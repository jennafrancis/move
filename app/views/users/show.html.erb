<h1><%= @user.email %></h1>

<h3><%= pluralize(@user.reviews.count, "Review") %> | <%= @user.average_rating %>/5 Average Rating</h3>

<h3>Reviews:</h3>

<%= render(@reviews) || "There are no reviews yet!" %>
